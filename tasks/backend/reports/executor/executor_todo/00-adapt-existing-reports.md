# Адаптировать существующие отчеты заказчика для перевозчика

## 📋 Обзор
Нужно адаптировать 10 существующих отчетов заказчика для работы с перевозчиком. Это не новая разработка, а изменение фильтров и логики доступа.

## 🎯 Что нужно изменить в каждом отчете:

### **1. Реестр заявок и перевозок**
**Адаптировать:** `01-reports-application-registry.md` → `01-executor-completed-transportation-registry.md`
**API Эндпоинт:** `GET /api/reports/executor/completed-transportation-registry`
**Тип:** GET-запрос с пагинацией

**Что менять:**
- **Фильтрация:** Заменить фильтр по `customerId` на `customerId` (заказчики перевозчика)
- **SQL:** Условие `tc.executor_organization_id = $executorId` вместо `t.organization_id = $organizationId`
- **Данные:** Показывать `customerName` вместо `executorName`
- **Права доступа:** Только перевозки где текущая организация = исполнитель

### **2. Финансовый отчет (оплаты от заказчика и МФО)**
**Адаптировать:** `05-reports-financial-payment.md` → `03-executor-financial-report.md`
**API Эндпоинт:** `GET /api/reports/executor/financial-report`
**Тип:** GET-запрос с агрегацией данных

**Что менять:**
- **Источник данных:** Объединить прямые платежи + выплаты от МФО
- **SQL:** Фильтровать по `tc.executor_organization_id`
- **Поля:** `source` = customer/mfo (источник платежа)
- **Агрегация:** Показывать поступления на счет перевозчика

### **3. Дебиторка заказчиков**
**Адаптировать:** `06-reports-debtor-analysis.md` → `04-executor-debtor-report.md`
**API Эндпоинт:** `GET /api/reports/executor/debtor-report`
**Тип:** GET-запрос с расчетом задолженностей

**Что менять:**
- **SQL:** Искать задолженности перед перевозчиком
- **Фильтр:** `tc.executor_organization_id = $executorId`
- **Логика:** Показывать кто должен перевозчику
- **Фьючерс:** Пока с моковыми данными, структура готова

### **4. SLA / дедлайны / опоздания**
**Адаптировать:** `09-reports-sla-performance.md` → `05-executor-sla-report.md`
**API Эндпоинт:** `GET /api/reports/executor/sla-report`
**Тип:** GET-запрос с PostGIS расчетами

**Что менять:**
- **SQL:** Фильтровать по `tc.executor_organization_id`
- **Перспектива:** Анализ производительности перевозчика
- **Метрики:** Показывать как сам перевозчик соблюдает сроки

### **5. Сравнение исполнителей (водителей/субподрядчиков)**
**Адаптировать:** `10-reports-executor-comparison.md` → `07-executor-driver-comparison-report.md`
**API Эндпоинт:** `GET /api/reports/executor/driver-comparison`
**Тип:** GET-запрос с аналитикой по водителям

**Что менять:**
- **SQL:** Искать водителей принадлежащих `executorId`
- **Данные:** Анализ собственных водителей перевозчика
- **Связи:** `employee_transport` + `organization_id = $executorId`

### **6. Гео-аналитика маршрутов**
**Адаптировать:** `03-reports-geo-analytics.md` → `08-executor-geo-analytics-report.md`
**API Эндпоинт:** `GET /api/reports/executor/geo-analytics-report`
**Тип:** GET-запрос с PostGIS и картами

**Что менять:**
- **SQL:** `tc.executor_organization_id = $executorId`
- **Данные:** Маршруты выполненные перевозчиком
- **Аналитика:** Эффективность маршрутов перевозчика

### **7. Логи подписей и ЭЦП**
**Адаптировать:** `11-reports-signature-logs.md` → `09-executor-signature-logs-report.md`
**API Эндпоинт:** `GET /api/reports/executor/signature-logs-report`
**Тип:** GET-запрос с фильтрацией по документам

**Что менять:**
- **SQL:** Фильтровать подписи где перевозчик = исполнитель
- **Объединение:** Контракты + АВР + Факторинговые соглашения
- **Доступ:** Только свои документы

### **8. Подписки / комиссии платформы**
**Адаптировать:** `07-reports-platform-commission.md` → `10-executor-commission-report.md`
**API Эндпоинт:** `GET /api/reports/executor/commission-report`
**Тип:** GET-запрос с расчетом комиссий

**Что менять:**
- **SQL:** Рассчитывать комиссии для перевозчика
- **Логика:** % от сделок перевозчика
- **Перспектива:** Показывать расходы перевозчика

### **9. Логи уведомлений**
**Адаптировать:** `12-reports-notification-logs.md` → `11-executor-notification-logs-report.md`
**API Эндпоинт:** `GET /api/reports/executor/notification-logs-report`
**Тип:** GET-запрос с фильтрацией по каналам

**Что менять:**
- **SQL:** Фильтровать `user_id` из сотрудников перевозчика
- **Подзапрос:** `WHERE e.organization_id = $executorId`
- **Данные:** Уведомления сотрудников перевозчика

### **10. Контракты и заявки**
**Адаптировать:** `04-reports-routes-contracts.md` → `14-executor-contracts-analysis-report.md`
**API Эндпоинт:** `GET /api/reports/executor/contracts-analysis`
**Тип:** GET-запрос с анализом контрактов

**Что менять:**
- **SQL:** `age.organization_id = $executorId` (исполнитель)
- **Связь:** `agreement_executor` таблица
- **Данные:** Контракты где перевозчик = исполнитель

## 🔧 **Техническая адаптация:**

### **1. Изменить базовый класс сервисов:**
```java
// Было (для заказчика)
@Query("SELECT t FROM Transportation t WHERE t.organizationId = :organizationId")

// Стало (для перевозчика)
@Query("SELECT t FROM Transportation t WHERE tc.executorOrganizationId = :executorId")
```

### **2. Обновить фильтры в DTO:**
```java
// Было
private Long customerId; // для заказчика
private Long executorId; // для фильтрации перевозчиков

// Стало
private Long customerId; // для фильтрации заказчиков перевозчика
// organizationId берем из security context
```

### **3. Изменить SQL запросы:**
```sql
-- Было (для заказчика)
WHERE t.organization_id = :organizationId

-- Стало (для перевозчика)
WHERE tc.executor_organization_id = :executorId
```

### **4. Обновить контроллеры:**
```java
// Было
@GetMapping("/applications/registry")
public ResponseEntity<?> getApplicationRegistry(
    @RequestParam(required = false) Long executorId
) // заказчик фильтрует перевозчиков

// Стало
@GetMapping("/executor/completed-transportation-registry")
public ResponseEntity<?> getCompletedTransportationRegistry() {
    // executorId берем из security context
} // перевозчик видит только свои данные
```

## 📝 **Чек-лист адаптации для каждого отчета:**

- [ ] **Изменить SQL запросы** - заменить `organization_id` на `executor_organization_id`
- [ ] **Обновить фильтры** - убрать фильтрацию по перевозчикам, добавить по заказчикам
- [ ] **Изменить perspective** - данные с точки зрения перевозчика
- [ ] **Обновить DTO** - поменять поля где нужно (executorName → customerName)
- [ ] **Проверить безопасность** - доступ только к своим данным
- [ ] **Обновить URL** - добавить `/executor/` префикс
- [ ] **Тесты** - проверить что работает фильтрация
- [ ] **Документация** - обновить примеры response

## ⚡ **Быстрый старт адаптации:**

### **Шаблон адаптации SQL:**
```sql
-- НАЙТИ: WHERE t.organization_id = :organizationId
-- ЗАМЕНИТЬ НА: WHERE tc.executor_organization_id = :executorId

-- НАЙТИ: LEFT JOIN user.organization o ON tc.executor_organization_id = o.id
-- ЗАМЕНИТЬ НА: LEFT JOIN user.organization o ON t.organization_id = o.id
```

### **Шаблон адаптации Java:**
```java
// НАЙТИ: @Param("organizationId") Long organizationId
// ЗАМЕНИТЬ НА: @Param("executorId") Long executorId

// НАЙТИ: o.organization_name as executorName
// ЗАМЕНИТЬ НА: o.organization_name as customerName
```

## 🚀 **Оценка трудозатрат:**
- **1 отчет:** ~2-4 часа (SQL + Java + тесты)
- **10 отчетов:** ~20-40 часов (1 неделя)
- **Сложные отчеты** (Гео-аналитика, SLA): ~6-8 часов каждый

## ✅ **Результат:**
После адаптации у перевозчика будут работать все 10 отчетов с правильной фильтрацией и доступом к данным.
