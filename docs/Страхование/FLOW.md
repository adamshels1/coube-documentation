# Страхование — Результаты тестирования (03.04.2026)

## Шаг 1 — Проверка компании (its_get_customer_ul)
**Статус: ⚠️ ПРОБЛЕМА НА СТОРОНЕ EURASIAN**

- БМГ/ГБД проходит ✅
- Зависает на `customer_send_save1c` (сохранение в 1С)
- Eurasian смотрят в понедельник (06.04.2026)
- Поле `leader` передавать НЕ нужно — вызывает ошибку `leader_short`

---

## Шаг 2 — Проверка физлица (its_get_customer)
**Статус: ✅ РАБОТАЕТ**

- Передаём ИИН + телефон (должен быть зарегистрирован в БМГ)
- Eurasian проверяет через БМГ ✅
- Отвечаем `{ kdp: 5 }` на шаг `kdp_get_type` ✅
- Получаем `system_data` физлица ✅
- Тестовый ИИН: `930419301377`, телефон: `77478777626`

---

## Шаг 3 — Оформление страховки (its_conclusion_cargo)
**Статус: ✅ РАБОТАЕТ**

- Передаём `client` + `insureds` (из шага 1), `beneficiaries` (из шага 2), данные груза
- Eurasian генерирует два PDF: Заявление + Договор ✅
- Тестовый `inst_id`: `16037637`

---

## Шаг 4 — Подтверждение документов (docs_get_confirm)
**Статус: ✅ РАБОТАЕТ**

- Получаем PDF в base64 (application + contract) ✅
- Подтверждаем через `{ confirm: 1 }` — НЕ `success: 1` ✅

---

## Шаг 5 — Отправка подписанных документов (docs_get_signed)
**Статус: ⚠️ ПРОБЛЕМА НА СТОРОНЕ EURASIAN**

- PDF подписан реально через NCALayer (CMS подпись) ✅
- Eurasian принимает параметры (`step_status: 2`) ✅
- Зависает на обработке — скорее всего та же проблема с 1С
- `system_id` НЕ нужен — это была ошибка в старой документации
- `invoice` — передаём пустую строку `""`

---

## Итог

```
⚠️  Шаг 1: its_get_customer_ul  — зависает в 1С (Eurasian смотрят 06.04.2026)
✅  Шаг 2: its_get_customer     — работает полностью
✅  Шаг 3: its_conclusion_cargo — работает полностью
✅  Шаг 4: docs_get_confirm     — работает, { confirm: 1 }
⚠️  Шаг 5: docs_get_signed      — принимает CMS PDF, зависает в 1С
❓  Шаг 6: step_status: 10      — не дошли, неизвестно что приходит в params
```

---

## Ждём от Eurasian

- Починить `customer_send_save1c` для юрлиц и финального сохранения договора
- Что приходит в `params` при `step_status: 10`
